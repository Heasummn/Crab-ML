extern pint(x: int): int;

operator +!(x: int, y: int): int = 
    x + y + 1

operator !(x:int, y: int): int = 
    y

def foo(x: int, y: int): int =
    y;

def bar(foobar: int): int =
    let x: int = foobar in
        let x: int = 5 in
            x + (x + foobar);

def afunc(): int = 
    3 +! 5;        

def main(): int = 
    pint(5) ! (5 + 4) ! pint(10)
