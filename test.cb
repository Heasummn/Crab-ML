operator +!(x: int, y: int): int = 
    x + y + 1


def foo(x: int, y: float): float =
    y;

def bar(foobar: int): int =
    let x: int = foobar in
        let x: int = 5 in
            x + (x + foobar);

def afunc(): int = 
    3 +! 5;        

def main(): int = 
    5;
