type id = int;

extern pint(x: id): int
extern pbool(x: bool): int

operator +! (x: int, y: int): int =
    x + y + 1

operator ! (x: int, y: int): int = 
	y

def afunc(): int = 
    3 +! 5;        

def main(): int = 
   pbool(4 > 5) ! pbool(4 < 5)
